<h1>Listing page_plugin_relations</h1>

<table>
  <tr>
    <th>Pid</th>
    <th>Plugin name</th>
    <th>Plugin params</th>
    <th>Plugin returned html</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @page_plugin_relations.each do |page_plugin_relation| %>
  <tr>
    <td><%= page_plugin_relation.pid %></td>
    <td><%= page_plugin_relation.plugin_name %></td>
    <td><%= page_plugin_relation.plugin_params %></td>
    <td><%= page_plugin_relation.plugin_returned_html %></td>
    <td><%= link_to 'Show', page_plugin_relation %></td>
    <td><%= link_to 'Edit', edit_page_plugin_relation_path(page_plugin_relation) %></td>
    <td><%= link_to 'Destroy', page_plugin_relation, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Page plugin relation', new_page_plugin_relation_path %>
