<h1>Listing news_trends</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @news_trends.each do |news_trend| %>
  <tr>
    <td><%= news_trend.name %></td>
    <td><%= link_to 'Show', news_trend %></td>
    <td><%= link_to 'Edit', edit_news_trend_path(news_trend) %></td>
    <td><%= link_to 'Destroy', news_trend, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New News trend', new_news_trend_path %>
